# iconmod use

# Install

```bash
npm i @iconmod/use
```

# Usage

```ts
import { setConfig } from '@iconmod/use'

setConfig({ provider: 'http://iconmod-provider.com', assetPath: 'assets/iconmod' })
```

## Web Component

```html
<IconmodIcon name="fp:google" online />
```
